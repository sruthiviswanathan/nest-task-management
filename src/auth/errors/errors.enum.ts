export enum ErrorCodes {
    UNIQUE_CONSTRAINT_ERR_CODE = '23505'
}

export enum ErrorMessages {
    UNIQUE_CONSTRAINT_ERR_MESSAGE = 'Duplicate key value violates unique constraint',
    USER_NOT_FOUND_ERR_MESSAGE = 'Invalid credentials.'
}